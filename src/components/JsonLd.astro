---
import { buildGraph, type PageMeta } from '../lib/seo/buildGraph';

const { page } = Astro.props as { page: PageMeta };
const graph = buildGraph(page);
---
<script type="application/ld+json" is:inline set:html={JSON.stringify(graph)} />
